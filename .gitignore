.DS_Store
node_modules/
.vercel/
.env
.env.*
# br doctor per-run artifacts
.doctor/
.vercel
.env*

# bv (beads viewer) local config and caches
.bv/

# GitHub social-preview card: uploaded via repo Settings (see ops/console-notes.md);
# the local copy stays on disk as the source file but is not tracked.
gh_og_share_image.png
