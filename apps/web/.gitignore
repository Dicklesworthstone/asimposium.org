# Next.js build output
.next/
out/
next-env.d.ts

# TypeScript incremental build state (next build sets `incremental: true`)
*.tsbuildinfo

# Dependencies
node_modules/

# The canonical lockfile is the bun-workspaces lockfile at the repository root.
# A nested lockfile here would shadow it and desynchronise the workspace.
bun.lock
bun.lockb

# Vercel
.vercel/
